import 'package:flutter/material.dart';

const kMaterialThemeSourceColor = Color(0xff6750A4);

const TextStyle kNotImplementedPlaceholderTextStyle = TextStyle(
  fontSize: 30,
  fontWeight: FontWeight.bold,
);

const EdgeInsets kScreenPadding = EdgeInsets.all(8.0);

const kMinimumVisualDensity = VisualDensity(
  vertical: VisualDensity.minimumDensity,
  horizontal: VisualDensity.minimumDensity,
);

const kMinimumVerticalVisualDensity = VisualDensity(
  vertical: VisualDensity.minimumDensity,
);

const kZeroSymmetricPadding = EdgeInsets.symmetric(
  horizontal: 0.0,
  vertical: 0.0,
);

const kZeroHorizontalSymmetricPadding = EdgeInsets.symmetric(
  horizontal: 0.0,
);

const kComponentPadding = EdgeInsets.all(16.0);
